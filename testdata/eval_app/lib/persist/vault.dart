// Eval fixture: storage node. The only hint is the /persist/ path segment —
// no repository/datasource/store/dao vocabulary.
class Vault {
  void put(String key) {
    // write-through, no return
  }
}
