// Eval fixture: outbound integration. No api/client/http keywords on purpose;
// classification relies on the side-effect step evidence.
class Gateway {
  void send(String payload) {
    // opens a socket and transmits
  }
}
