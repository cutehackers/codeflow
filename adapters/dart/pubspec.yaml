name: codeflow_dart_adapter
description: Evidence-first Dart structural analysis for CodeFlow.
version: 0.5.0
publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  analyzer: ^8.0.0

dev_dependencies:
  test: ^1.25.0
