{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://codeflow.local/schemas/document-revision.schema.json",
  "title": "CodeFlow Document Revision",
  "$comment": "RFL-SC R2 VS-01 canonical schema for immutable DocumentRevision.",
  "type": "object",
  "required": [
    "schemaId",
    "schemaVersion",
    "revisionId",
    "path",
    "documentVersion",
    "contentId",
    "source",
    "workspaceEpoch",
    "createdAt"
  ],
  "additionalProperties": false,
  "properties": {
    "schemaId": {
      "type": "string",
      "enum": [
        "https://codeflow.local/schemas/document-revision.schema.json",
        "codeflow.document-revision",
        "https://codeflow.local/schemas/rflsc.document-revision.v2.schema.json"
      ]
    },
    "schemaVersion": { "const": 2 },
    "revisionId": { "type": "string", "minLength": 1, "maxLength": 128 },
    "path": { "type": "string", "minLength": 1 },
    "documentVersion": { "type": "integer", "minimum": 1 },
    "contentId": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "source": {
      "type": "string",
      "enum": ["agent_transaction", "ide_versioned", "watcher_fallback", "repository_capture"]
    },
    "workspaceEpoch": { "type": "integer", "minimum": 0 },
    "createdAt": { "type": "string", "format": "date-time" },
    "content": { "type": "string" },
    "byteLength": { "type": "integer", "minimum": 0 }
  }
}
