{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://codeflow.local/schemas/change-batch.schema.json",
  "title": "CodeFlow Change Batch",
  "$comment": "RFL-SC R2 VS-01 canonical schema for a committed multi-file ChangeBatch.",
  "type": "object",
  "required": [
    "schemaId",
    "schemaVersion",
    "batchId",
    "source",
    "workspaceEpoch",
    "revisions",
    "status",
    "createdAt"
  ],
  "additionalProperties": false,
  "properties": {
    "schemaId": {
      "type": "string",
      "enum": [
        "https://codeflow.local/schemas/change-batch.schema.json",
        "codeflow.change-batch",
        "https://codeflow.local/schemas/rflsc.change-batch.v2.schema.json"
      ]
    },
    "schemaVersion": { "const": 2 },
    "batchId": { "type": "string", "minLength": 1, "maxLength": 128 },
    "transactionId": { "type": ["string", "null"] },
    "source": {
      "type": "string",
      "enum": ["agent_transaction", "ide_versioned", "watcher_fallback", "repository_capture"]
    },
    "workspaceEpoch": { "type": "integer", "minimum": 0 },
    "revisions": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 }
    },
    "status": {
      "type": "string",
      "enum": ["open", "committed", "aborted"]
    },
    "createdAt": { "type": "string", "format": "date-time" },
    "committedAt": { "type": ["string", "null"], "format": "date-time" }
  }
}
